source "https://rubygems.org"

# `github-pages` pins Jekyll and all its dependencies to the exact
# versions GitHub Pages runs server-side. Always go through this gem
# rather than installing Jekyll directly, so local builds match what
# Pages actually serves. See https://pages.github.com/versions/ for
# the currently pinned versions.
gem "github-pages", group: :jekyll_plugins
